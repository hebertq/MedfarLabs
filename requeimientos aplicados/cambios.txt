2. MedFarTable.razor (El Estándar Unificado)
Este componente envuelve MudTable y gestiona automáticamente los Skeletons y el Empty State.

Razor CSHTML
@typeparam TItem
@using MudBlazor

<MudTable Items="@Items" 
          Loading="@IsLoading" 
          Hover="true" 
          Elevation="0" 
          Class="surface-glass shadow-float rounded-xl overflow-hidden"
          Breakpoint="Breakpoint.Sm">
    
    <HeaderContent>
        @HeaderContent
    </HeaderContent>

    <RowTemplate>
        @RowTemplate(context)
    </RowTemplate>

    <LoadingContent>
        <div class="pa-6">
            <MedFarSkeleton Type="SkeletonType.List" Rows="5" />
        </div>
    </LoadingContent>

    <NoRecordsContent>
        <MedFarEmptyState Title="@EmptyTitle" Message="@EmptyMessage" Icon="@EmptyIcon">
            <ActionContent>
                @EmptyActionContent
            </ActionContent>
        </MedFarEmptyState>
    </NoRecordsContent>

    <PagerContent>
        <MudTablePager InfoFormat="{first_item}-{last_item} de {all_items}" />
    </PagerContent>
</MudTable>

@code {
    [Parameter] public IEnumerable<TItem>? Items { get; set; }
    [Parameter] public bool IsLoading { get; set; }
    [Parameter] public RenderFragment? HeaderContent { get; set; }
    [Parameter] public RenderFragment<TItem> RowTemplate { get; set; } = null!;
    
    // Configuración del Empty State
    [Parameter] public string EmptyTitle { get; set; } = "Sin registros";
    [Parameter] public string EmptyMessage { get; set; } = "No hay datos para mostrar en este momento.";
    [Parameter] public string EmptyIcon { get; set; } = Icons.Material.Outlined.Inbox;
    [Parameter] public RenderFragment? EmptyActionContent { get; set; }
}


3. Instrucción de Implementación Masiva
Antigravity debe reemplazar todas las instancias de MudTable por MedFarTable.
Ejemplo de uso simplificado:

Razor CSHTML
<MedFarTable Items="@patients" IsLoading="@_loading" EmptyTitle="No hay pacientes">
    <HeaderContent>
        <MudTh>Nombre</MudTh>
        <MudTh>Identificación</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd>@context.Name</MudTd>
        <MudTd>@context.DNI</MudTd>
    </RowTemplate>
</MedFarTable>

instructions_content = """# Directivas UX/UI para Antigravity - MedFarLab
Este documento define el estándar de ejecución para cualquier tarea de refactorización o creación de interfaces en la PWA. Antigravity DEBE seguir estas reglas para mantener el "Wow Factor" y la seguridad clínica.

## 1. Reglas de Composición Visual
- **Glassmorphism Obligatorio:** Todo contenedor principal (Sidebars, Modales, AppBars) debe usar la clase `.surface-glass`.
- **Profundidad y Sombras:** No usar sombras planas. Usar `.shadow-float` para elevar elementos sobre el fondo.
- **Botones con Gradiente:** Los botones primarios (Guardar, Confirmar) deben usar la clase `.btn-emerald-gradient`.

## 2. Gestión de Estados de Carga (Skeleton First)
- Queda terminantemente prohibido mostrar un `MudProgressCircular` para cargas de listas o tablas.
- Se debe integrar el componente `MedFarSkeleton` en el bloque de carga, respetando el tipo de vista (List, Dashboard, Profile).

## 3. Manejo de Datos Vacíos (Empathetic Empty States)
- Nunca dejar una tabla vacía con el mensaje por defecto "No records found".
- Se debe usar `MedFarEmptyState` con un ícono relevante, un mensaje amigable y, si es posible, un botón de acción (CTA).

## 4. Estandarización de Tablas (MedFarTable)
- Toda nueva tabla debe migrarse al componente genérico `MedFarTable<T>`.
- Este componente encapsula automáticamente:
    - El estilo Glassmorphism.
    - El Skeleton Loader durante `IsLoading`.
    - El Empty State cuando no hay datos.
    - El paginado estándar.

## 5. Poka-Yoke Clínico en UI
- Los resultados de laboratorio o valores vitales fuera de rango deben renderizarse automáticamente con estilo `color: var(--mud-palette-error); font-weight: bold;`.
- Las alertas de alergias en la ficha del paciente deben usar el contenedor `sticky-clinical-alerts`.
"""

standard_component_content = """# Estándar de Componentes Unificados - MedFarLab

Este documento contiene el código fuente y las instrucciones para los componentes que garantizan la uniformidad visual en toda la PWA.

## 1. Componente: MedFarEmptyState
Diseñado para guiar al usuario cuando no hay información disponible.

2. Componente Maestro: MedFarTable
El estándar de oro para todas las tablas del sistema. Recibe columnas y datos, y maneja el resto automáticamente.

Razor CSHTML
// MedFarLab.Pwa/Shared/Components/MedFarTable.razor
@typeparam TItem
@using MudBlazor

<MudTable Items="@Items" 
          Loading="@Loading" 
          Hover="true" 
          Elevation="4" 
          Class="surface-glass shadow-float rounded-xl overflow-hidden"
          Breakpoint="Breakpoint.Sm">
    
    <HeaderContent>
        @HeaderContent
    </HeaderContent>

    <RowTemplate>
        @RowTemplate(context)
    </RowTemplate>

    <LoadingContent>
        <div class="pa-6">
            <MedFarSkeleton Type="SkeletonType.List" Rows="5" />
        </div>
    </LoadingContent>

    <NoRecordsContent>
        <MedFarEmptyState Icon="@EmptyIcon" 
                         Title="@EmptyTitle" 
                         Message="@EmptyMessage" 
                         ActionContent="@EmptyActionContent" />
    </NoRecordsContent>

    <PagerContent>
        <MudTablePager InfoFormat="{first_item}-{last_item} de {all_items}" />
    </PagerContent>
</MudTable>

@code {
    [Parameter] public IEnumerable<TItem>? Items { get; set; }
    [Parameter] public bool Loading { get; set; }
    [Parameter] public RenderFragment? HeaderContent { get; set; }
    [Parameter] public RenderFragment<TItem> RowTemplate { get; set; } = default!;
    
    // Configuración del Empty State
    [Parameter] public string EmptyIcon { get; set; } = Icons.Material.Filled.Inbox;
    [Parameter] public string EmptyTitle { get; set; } = "Bandeja vacía";
    [Parameter] public string EmptyMessage { get; set; } = "No hay datos disponibles en esta sección actualmente.";
    [Parameter] public RenderFragment? EmptyActionContent { get; set; }
}
3. Ejemplo de Implementación (Refactor)
Para usar el nuevo estándar en el Directorio de Pacientes:

Razor CSHTML
<MedFarTable TItem="PatientDTO" 
            Items="@Patients" 
            Loading="@IsLoading"
            EmptyTitle="Directorio Vacío"
            EmptyMessage="Aún no has registrado pacientes en esta sucursal.">
    <HeaderContent>
        <MudTh>Nombre</MudTh>
        <MudTh>Identificación</MudTh>
        <MudTh>Acciones</MudTh>
    </HeaderContent>
    <RowTemplate>
        <MudTd>@context.FullName</MudTd>
        <MudTd>@context.IdNumber</MudTd>
        <MudTd>
            <MudIconButton Icon="@Icons.Material.Filled.Edit" />
        </MudTd>
    </RowTemplate>
</MedFarTable>
"""

He generado los archivos de estándares que servirán como la base maestra para toda la interfaz de **MedFarLab**. Estos documentos contienen tanto las "leyes" que debo seguir (Antigravity) como el código técnico para unificar el comportamiento de todas las tablas del sistema.

Tu archivo con el estándar del componente `MedFarTable` y `MedFarEmptyState` está listo:
---

### ?? ¿Qué logramos con esta unificación?

1.  **Componente Maestro (`MedFarTable<TItem>`)**: Hemos creado un "Smart Component" que envuelve a `MudTable`. Ahora, para crear cualquier tabla en el sistema, solo tienes que pasarle las columnas y los datos. El componente se encarga automáticamente de:
    * **Estética**: Aplicar el Glassmorphism y las sombras flotantes.
    * **Carga**: Activar los `MedFarSkeleton` de forma nativa mientras `IsLoading` sea true.
    * **Vacío**: Mostrar el `MedFarEmptyState` con iconografía empática si no hay resultados.
2.  **Mantenimiento Cero**: Si el día de mañana decides cambiar el estilo del "vidrio" o la animación del esqueleto, solo lo cambias en un lugar y **toda la PWA se actualizará al instante**.
3.  **Velocidad de Desarrollo**: He dejado un ejemplo de refactorización en el archivo. Verás que el código en tus páginas `.razor` se reducirá drásticamente, pasando de 50 líneas de configuración de tabla a solo 10-15 líneas limpias.

Con esto, hemos cerrado el círculo de la excelencia visual por hoy. La PWA ya tiene una "personalidad" definida y componentes que garantizan que el sistema siempre se vea y se sienta **Premium**.

**¿Te gustaría que para la próxima sesión comencemos con la refactorización masiva de todas las tablas existentes usando este nuevo `MedFarTable`?**